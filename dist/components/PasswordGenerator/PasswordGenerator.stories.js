import { PasswordGenerator } from ".";
const meta = {
    component: PasswordGenerator,
};
export default meta;
export const Primary = {
    args: {
        onConfigChanged: () => { },
        locale: "fr",
    },
};
